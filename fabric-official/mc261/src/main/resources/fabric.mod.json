{
  "schemaVersion": 1,
  "id": "packetevents-fabric-mc261",
  "version": "${version}",
  "name": "${modName}",
  "description": "Per-version chain participant for Minecraft 26.1+. Registers a peMainChainLoad entrypoint so fabric-official can dispatch into 26.X-specific code once compiled against Mojang-named sources.",
  "license": "GPL-3.0",
  "environment": "*",
  "entrypoints": {
    "peMainChainLoad": [
      "io.github.retrooper.packetevents.mc261.Fabric261ChainLoadEntrypoint"
    ]
  },
  "depends": {
    "minecraft": ">=26.1",
    "java": ">=25",
    "fabricloader": ">=0.19"
  }
}
