org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# 1.16.1 is just the aggregator's Loom build target so Loom is happy; the real
# supported floor is 1.14 (the lowest intermediary module, mc1140).
# Per-variant MC versions live in fabric-intermediary/gradle.properties and
# fabric-official/gradle.properties.
minecraft_version=1.16.1
yarn_mappings=1.16.1+build.21
loader_version=0.16.14

publishing.skip_files=true
publishing.platform=fabric
publishing.modrinth.loaders=fabric
publishing.modrinth.version.start=1.14
publishing.modrinth.version.end=latest
